<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Recordings live in filesDir/recordings; shared via a temporary copy in cacheDir/shared. -->
    <files-path name="recordings" path="recordings/" />
    <cache-path name="shared" path="shared/" />
</paths>
